import React from "react";
import { HeaderDiv, Title } from "../styles/Header-styles";

const Header = () => {
  return (
    <div>
      <HeaderDiv>
        <Title>Weather App</Title>
      </HeaderDiv>
    </div>
  );
};

export default Header;
